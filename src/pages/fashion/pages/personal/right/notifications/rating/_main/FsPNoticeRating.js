import React from 'react';
import PropTypes from 'prop-types';

FsPNoticeRating.propTypes = {
    
};

function FsPNoticeRating(props) {
    return (
        <div>
            
        </div>
    );
}

export default FsPNoticeRating;