import React from 'react';
import PropTypes from 'prop-types';

FsPNoticeWallet.propTypes = {
    
};

function FsPNoticeWallet(props) {
    return (
        <div className="bg-primary">
            
        </div>
    );
}

export default FsPNoticeWallet;