import React from 'react';
import PropTypes from 'prop-types';

FsPNoticeActivity.propTypes = {
    
};

function FsPNoticeActivity(props) {
    return (
        <div>
            
        </div>
    );
}

export default FsPNoticeActivity;