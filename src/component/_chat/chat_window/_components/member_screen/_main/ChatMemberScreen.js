import React from 'react';
import PropTypes from 'prop-types';

// 
ChatMemberScreen.propTypes = {
    
};

// 
function ChatMemberScreen(props) {
    // 
    return (
        <div>
            
        </div>
    );
}

export default ChatMemberScreen;